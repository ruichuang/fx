export class Country {
    code: string;
    sub: string;
    name: string;

    constructor(code: string, sub: string, name: string) {
        this.code = code;
        this.sub = sub;
        this.name = name;
    }
}
